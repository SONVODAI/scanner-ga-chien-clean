# Foreign-flow alternative source audit — delivery

**Verdict:** `FOREIGN_FLOW_UNIVERSE_PROXY_FOUND`

SSI retired as preferred path after production VPS 403.  
Primary candidate: **official HSX** per-symbol foreign buy/sell **VALUE** (dated) → aggregate EMS **142** as `universe_foreign_*` (not HOSE-wide).  
Fallback: **vnstock 4.0.5 VCI** `price_board` VALUE columns (forward-only).

Full table + probes: `FOREIGN_FLOW_ALTERNATIVE_SOURCE_AUDIT.md`  
**No adapter implemented** (audit only).
