# Mr.BOT Intraday Memory V1A — collector dependencies
# Install separately from production requirements.txt to avoid breaking Legacy app.py
#
#   pip install -r requirements-collector.txt
#
# Production app.py uses the legacy git pin in requirements.txt (not vnstock 4.x).

vnstock>=4.0.5,<5
pyarrow>=14.0.0
